package com.micro.starter.dto;

public enum OrderStatus {
        registered,
        paid,
        payment_failed,
        invented,
        inventment_failed,
        delivered,
        delivery_failed,
        unexpected_failure
}